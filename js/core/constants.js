// =================================================
// FIXED SAFE ZONE SYSTEM - Competitive Fairness
// =================================================

// SAFE ZONE - identyczna dla wszystkich (fairness)
export const SAFE_ZONE_WIDTH = 1200;
export const SAFE_ZONE_HEIGHT = 600;

// PC vs Mobile different scale targets
export const PC_TARGET_WIDTH = 1000;   // Mniejszy safe zone na PC (lower res sprites)
export const MOBILE_TARGET_WIDTH = 1200; // Pełny safe zone na mobile
